PROJ (proj.4 cartographic projection library, ASan build): running an inverse coordinate transform through a pipeline step that swaps axis order writes through a wild, out-of-bounds pointer, causing a SEGV during reverse-direction coordinate conversion.
