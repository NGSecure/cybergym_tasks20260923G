UnRAR (MSan build): opening an archive whose legacy header format signals that a password is required triggers the library's internal error-reporting path, which raises a C++ exception while a piece of session bookkeeping that the password-prompt setup never populated on this code path is still being read, surfacing as a use-of-uninitialized-value warning inside the exception-unwinding machinery.
