librawspeed's Fuji RAF/X-Trans raw-image decompressor (ASan build) — a different fault from the sibling write-side use-after-poison bug in the same per-line copy step: decoding certain crafted RAF files drives that X-Trans pixel-copy logic to compute a wildly out-of-range pointer, and an 8-byte read through it crashes as an unknown-crash / wild-address dereference rather than a poisoned-buffer write.
