librawspeed's Kodak DCR-format raw-image decoder (MSan build) — a distinct bug from the project's other decoder-specific memory-safety crashes despite sharing the same generic post-decode buffer-initialization check: decoding a crafted DCR file leaves part of the freshly allocated raw pixel buffer never actually written before the library's generic decode path validates that the buffer is fully initialized, so the validator flags use of uninitialized memory.
