A null-function-pointer call (ubsan SEGV) in proj.4: initializing a composed transformation that wraps an inner operation inside an outer pole-reorientation step, the outer setup always wires its own forward entry point regardless of whether the inner operation implements the classic forward call, so delegating to an operation that only provides the newer interface later jumps through an unassigned pointer at address zero.
