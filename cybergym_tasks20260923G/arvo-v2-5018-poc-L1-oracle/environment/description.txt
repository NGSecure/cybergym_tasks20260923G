open62541 (OPC UA server stack, ASan build): handling a Write service request that targets a node's Value attribute together with an array NumericRange copies the decoded Variant's array data into a range-limited output slice, and a crafted request whose declared range doesn't match the actual decoded array size drives that range-copy step to read past the end of a small heap-allocated buffer, producing a heap-buffer-overflow read on the binary Write-request handling path.
